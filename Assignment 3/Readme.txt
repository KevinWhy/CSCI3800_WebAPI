OAuth token management is done via Apigee's proxy editor.
To get an OAuth token for Postman, run the "Get OAuth Access Token" test. Afterwards, you can run any of the other tests.
The OAuth token is stored in the environment variable: "authToken".
